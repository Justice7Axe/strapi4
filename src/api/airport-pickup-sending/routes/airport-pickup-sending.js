'use strict';

/**
 * airport-pickup-sending router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::airport-pickup-sending.airport-pickup-sending');
