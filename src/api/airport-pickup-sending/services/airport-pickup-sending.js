'use strict';

/**
 * airport-pickup-sending service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::airport-pickup-sending.airport-pickup-sending');
